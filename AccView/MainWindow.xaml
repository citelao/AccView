<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AccView.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AccView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AccView">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid
        Loaded="Grid_Loaded">
        <TextBox
            x:Name="OutputTextBlock"
            AcceptsReturn="True"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            Margin="8" />

        <TreeView x:Name="ElementsTreeView" ItemsSource="{x:Bind TreeViewItems}">
            <TreeView.ItemTemplate>
                <DataTemplate x:DataType="TreeViewNode">
                    <TreeViewItem ItemsSource="{x:Bind Children}" Content="{x:Bind Content}" />
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
